# R5-C (rvec side) — descriptive geometry of the r-fiber, corrected capture

**Status: the registered R5-C predictions remain OPEN.** They are
hidden-state statements — carrier share of *hidden* variance < 1%,
intrinsic-dimension/rotation discontinuities of the *hidden* stream — and
stay gated on the widened pass (D1). This report is the rvec-side geometry
the ledger unblocked, computed on the corrected four-arm capture. Nothing
here evaluates a registered threshold; LF8's fiber predictions (defined on
the six v1 texts) are likewise untouched.

## Descriptives (66 layers × 4 arms, flattened 1024-dim fiber)

- **The carrier dominates the live fiber.** The mean r-vector direction
  carries a median **67%** of the fiber's second moment (range 39–91%).
  Most carrier-dominated: L65 (0.90) and L5 (0.77) — the two seam-sensitive
  layers. The weight-side "communal carrier" finding, now measured in vivo
  on corrected data.
- **Fluctuations are low-dimensional:** participation ratio median **17.7**
  of 1024 (range 7–64). The fiber is a thin cone around the carrier.
- **Orientation is content-stable:** cross-arm cos(mean-r) median **0.93**
  across the four arms (chat/math/multi-chat/prose).
- **One anomaly flagged for LF8 proper: L53.** Its minimum cross-arm cos is
  **0.54** — by far the most content-dependent carrier orientation in the
  network (next lowest layers sit near the 0.93 median). Whatever L53's
  carrier encodes, it is not purely positional. Recorded as a descriptive
  flag; LF8's registered evaluation on the v1 texts should look here first.
- Adjacent-layer carrier rotations are mostly aligned, with a handful of
  strong sign flips (L45→46 cos −0.39, L43→44 −0.27, L52→53 −0.23) — the
  deep-trunk carrier re-orients in steps, not smoothly.

Full tables: [`r5c_rvec_side.json`](r5c_rvec_side.json). Script:
`scripts/round5_r5c_rvec_side.py` (reads only the corrected capture; hashes
recorded).
